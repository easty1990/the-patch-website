{
  "name": "project-name",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "@agent/setupTests": "^1.0.0"
  },
  "devDependencies": {
    "playwright": "^1.0.0"
  }
}